# Intermittent 502 Bad Gateway errors when accessing backend APIs through Nginx - Task Summary

## 快照
Intermittent 502 Bad Gateway errors when accessing backend APIs through Nginx → Nginx caches upstream container IP addresses. When containers are recreated by Docker Compose, their internal IPs may change, but Nginx continues to use the stale (cached) IPs. → See solution doc for fix steps → ✓ Resolved

## 诊断流程

```mermaid
graph TD
    A[Problem Detected] --> B{{Investigate}}
    B --> C[Root Cause: Nginx caches upstream container IP addresses. When containers are recreated by Docker Compose, their internal IPs may change, but Nginx continues to use the stale (cached) IPs.]
    C --> D[Apply Fix]
    D --> E[Verify Solution]
    E --> F[✓ Resolved]
    
    style F fill:#90EE90
```

## 关键信息

| 项目 | 内容 |
|------|------|
| **模块** | docker-infrastructure |
| **严重性** | high |
| **根本原因** | Nginx caches upstream container IP addresses. When containers are recreated by Docker Compose, their internal IPs may change, but Nginx continues to use the stale (cached) IPs. |
| **解决方案** | See solution doc for fix steps |
| **标签** | `nginx`, `docker-compose`, `502-bad-gateway`, `dns-cache` |

## 快速参考

**一键修复**:
```bash
See solution doc for fix steps
```

## 参考链接

- [完整 Solution Doc](g:\Workspace\deer-flow\docs\solutions\502-bad-gateway-dns-cache.md)

---

✨ **Generated by**: Compound Engineering + Antigravity Integration  
📅 **Date**: 2026-03-10  
🏷️ **Tags**: nginx, docker-compose, 502-bad-gateway, dns-cache
